{% extends 'base.html.twig' %}

{% block title %}Inscription - Mon Coin Cuisine{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-100 flex items-center justify-center p-4">

    <div class="w-full max-w-lg bg-white p-8 sm:p-10 rounded-xl shadow-2xl space-y-6 transform transition duration-500 ease-in-out hover:shadow-xl">
        
        <h1 class="text-3xl font-extrabold text-center text-gray-900 mb-6">
            Créer votre Compte Cuisinier 🧑‍🍳
        </h1>

        {% for flash_error in app.flashes('verify_email_error') %}
            <div class="p-4 mb-4 text-sm text-red-800 rounded-lg bg-red-50" role="alert">
                {{ flash_error }}
            </div>
        {% endfor %}

        {{ form_start(registrationForm, {'attr': {'class': 'space-y-6'}}) }}

            {{ form_errors(registrationForm) }}

            <div class="space-y-4">
                {# Champ Prénom #}
                {{ form_row(registrationForm.firstName, {
                    'label': 'Prénom',
                    'row_attr': {'class': 'form-group'},
                    'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'},
                    'attr': {'class': 'appearance-none block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-orange-500 focus:border-orange-500 sm:text-sm transition'}
                }) }}

                         {# Champ Nom#}
                {{ form_row(registrationForm.secondName, {
                    'label': 'Nom',
                    'row_attr': {'class': 'form-group'},
                    'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'},
                    'attr': {'class': 'appearance-none block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-orange-500 focus:border-orange-500 sm:text-sm transition'}
                }) }}

                {# Champ Email #}
                {{ form_row(registrationForm.email, {
                    'row_attr': {'class': 'form-group'},
                    'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'},
                    'attr': {'class': 'appearance-none block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-orange-500 focus:border-orange-500 sm:text-sm transition'}
                }) }}
                
                {# Champ Mot de passe #}
                {{ form_row(registrationForm.plainPassword, {
                    'label': 'Mot de Passe',
                    'row_attr': {'class': 'form-group'},
                    'label_attr': {'class': 'block text-sm font-medium text-gray-700 mb-1'},
                    'attr': {'class': 'appearance-none block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:outline-none focus:ring-orange-500 focus:border-orange-500 sm:text-sm transition'}
                }) }}
            </div>
            


            <button 
                type="submit" 
                class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-lg font-medium text-white bg-orange-600 hover:bg-orange-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-orange-500 transition duration-150 ease-in-out transform hover:scale-[1.005]"
            >
                S'inscrire
            </button>
            
        {{ form_end(registrationForm) }}

        <div class="mt-6 text-center text-sm">
            <p class="text-gray-600">
                Vous avez déjà un compte ? 
                <a href="{{ path('app_login') }}" class="font-medium text-orange-600 hover:text-orange-500">Connectez-vous ici</a>
            </p>
        </div>

    </div>
</div>
{% endblock %}
